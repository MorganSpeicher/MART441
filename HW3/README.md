For this assignment I created a very simple html webpage that has a light pink background and a hero image, just to have something displayed on the page. In my .js file I used the console.log() function to answer each question individually, breaking up the larger question such as “what is your favorite artist and what is their cultural significance” into separate console.log() functions. For the prompt() function I used a variable to store the answer and prompt the dialog box into two functions, then had the console log display those variable answers and questions.

The Generative AI created a simple html page, but instead of having an image be displayed it chose to have two small lines of text that went along the lines of “Welcome to my page” and “view the console to see my javascript output”. The AI made an extensive .js file where every answer to each question and its parts were broken up into separate console.log() functions, as well as a header for each question. It also used the “let” function and a variable instead to prompt the user dialog box then display both of those inputs in the console log.

I feel that if I relied solely on generative AI output I wouldn’t learn anything substantially. I like to use as minimal AI as possible, and also, I understand that you learn as you do, and making mistakes/troubleshooting builds a much deeper understanding of the material than a prompt of an AI ever could.

I do feel like I could adequately describe the code that both I, and the gen. AI wrote. I did need to google how to do the prompt() function, but I went through iterations and found a way that worked in a way that I understood. I used Youtube to help get the basics, then spent the time to understand what I was actually writing.

The final work most definitely still feels like my own, the only thing I forgot to include in my original code that I remembered after prompting the AI was to add the lang=”en” to the html tag and meta charset="UTF-8" inside the head tag.
